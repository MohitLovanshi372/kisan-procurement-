Place your uploaded background image here and name it `site-background.jpg` (recommended) so the site CSS can load it.

Path: frontend/assets/site-background.jpg

Recommended size: 1920x1080 or similar, optimized for web (compress to <500KB if possible).

After adding the image, reload the frontend pages to see the background.
